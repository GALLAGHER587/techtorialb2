# Given two strings, append them together ("Known as concatenation)
# and return the result. However, if the the strings are differernt lengths,
# omit chars from the longer string so it is the same length as the shorter string.
# So "Hello" and "Hi" yeild "loHi". The strings may be any length 

